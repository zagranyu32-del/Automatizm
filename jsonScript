let Activeted = 0;
document.addEventListener('keydown', async function(e) {
  if (e.key === 'p') {
    Activeted++;
    if (Activeted === 3) {
      Activeted = 0;
      let value = prompt('Enter command:');
      // ...
    }
  } else {
    Activeted = 0;
  }
});
const scripts = {
  qa: 'BestScript.js',
  test: 'testing.js'
};

if (scripts[value]) {
  await ImportScript(scripts[value]);
} else {
  console.log('Unknown command');
}
async function ImportScript(file) {
  const url = `https://raw.githubusercontent.com/zagranyu32-del/Automatizm/main/${file}`;
  try {
    const res = await fetch(url);
    if (!res.ok) throw new Error(res.status);
    const code = await res.text();
    eval(code); // Выполнение загруженного JavaScript-кода
    console.log(`${file} loaded`);
  } catch(e) {
    console.error('Error import:', e);
  }
}